<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Pest bootstrap
|--------------------------------------------------------------------------
| Global test configuration. Bind a base TestCase or shared helpers here as
| the suite grows. The reflection cache in EnumInspector is process-static,
| so tests that depend on cache state should reset it explicitly.
*/

// uses()->in('Feature');
